# секреты и локальные артефакты — в репозиторий не попадают
.env
# бэкапы и варианты (.env.bak.*, .env.local, .env.prod) — тоже секреты
.env.*
# ...но шаблон без секретов оставляем в репо (образец для деплоя)
!.env.example
*.session
*.db

# python
__pycache__/
*.pyc

# логи прогонов
*.log
