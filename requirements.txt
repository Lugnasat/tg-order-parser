# ---------------------------------------------
# Зависимости freelance-lead-parser
# Версии запинены по образцу «Агрегатор» (на чём он реально работал).
# Пин обязателен: aiogram 3.7+ перенёс parse_mode в DefaultBotProperties,
# без фиксации версии вызов send_message(parse_mode=...) повёл бы себя
# иначе (правка П9).
# ---------------------------------------------
telethon==1.42.0
aiogram==3.25.0
python-dotenv==1.2.1
